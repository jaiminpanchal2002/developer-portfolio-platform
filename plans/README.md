# Animation improvement plans

Generated by the `improve-animations` audit against commit `d0a18b9`. Each
plan is self-contained — see the individual file for full context.

| # | Title | Severity | Status |
|---|-------|----------|--------|
| [001](001-gate-aurora-background-to-public-pages.md) | Gate the aurora background to public pages only | HIGH | TODO |
| [002](002-bento-card-transition-all.md) | Replace `.bento-card`'s `transition: all` with an explicit property list | HIGH | TODO |
| [003](003-navbar-underline-scalex.md) | Fix Navbar hover underline: `width` → `transform: scaleX()` (also fixes a hidden hover-effect bug) | HIGH | TODO |
| [004](004-magnetic-button-transform-string.md) | Drive `MagneticButton` via a `transform` string instead of the `x`/`y` shorthand | HIGH | TODO |
| [005](005-skillgalaxy-pause-offscreen.md) | Pause `SkillGalaxy`'s WebGL render loop when scrolled out of view | HIGH | TODO |
| [006](006-admin-reduced-motion.md) | Respect `prefers-reduced-motion` across the entire admin panel via `MotionConfig` | HIGH | TODO |
| [007](007-sectionwrapper-remove-double-animation.md) | Stop `SectionWrapper` from double-animating every section's reveal | MEDIUM | TODO |
| [008](008-reduced-motion-entrance-animations.md) | Respect `prefers-reduced-motion` in entrance animations across 8 public-site files | MEDIUM | TODO |
| [009](009-skillgalaxy-dustfield-reduced-motion.md) | Gate `SkillGalaxy`'s `DustField` rotation on `reduceMotion` | MEDIUM | TODO |
| [010](010-admin-layout-property-animations.md) | Replace admin `width`/`height` layout-property animations | MEDIUM | TODO |
| [011](011-admin-transition-all-and-easing-tokens.md) | Replace admin `transition-all` and hand-rolled easings with explicit properties / shared tokens | MEDIUM | TODO |
| [012](012-admin-drop-infinite-decorative-loops.md) | Drop permanent decorative `animate-bounce`/`-ping`/`-pulse` flourishes in admin | LOW | TODO |
| [013](013-atsmatcher-token-drift-and-loading-noise.md) | Fix `AtsMatcher.tsx` easing/token drift and its triple-loop loading state | LOW | TODO |

## Execution order

001-013 touch different files and are fully independent — any order, or all in parallel. (008 touches `Hero.tsx`, `About.tsx`, `Certificates.tsx`, `Education.tsx`, `Experience.tsx`, `Testimonials.tsx`, `GitHubShowcase.tsx`, `ImageLightbox.tsx`; 009 touches `SkillGalaxy.tsx` — same file as 005, but 005 only adds a wrapper `div`/`frameloop` prop while 009 only edits `DustField`, so they don't conflict; still worth applying 005 before 009 to avoid re-diffing the same file twice. 010 touches `app/admin/page.tsx`, `app/admin/jobs/page.tsx`, `BulkActionBar.tsx`. 011 touches `app/admin/applications/page.tsx`, `app/admin/jobs/page.tsx`, `app/admin/resume/page.tsx`, `AnimatedModal.tsx`, `app/admin/interview/page.tsx`, `app/admin/security/page.tsx` — note 011 shares `app/admin/jobs/page.tsx` with 010, but at different, non-overlapping lines (010 edits the match-score progress bar at lines 270-277; 011 edits the "Find Jobs" submit button's className at line 188), so no conflict. 012 touches `app/admin/inquiries/page.tsx`, `app/admin/profile-score/page.tsx`. 013 touches `AtsMatcher.tsx` — no overlap with any other plan.)

## Backlog (not yet turned into plans)

Every finding from the original audit table (#1-13) now has a plan. Only
the 4 "missed opportunity" items remain unplanned, available on request:
ImageLightbox slide transitions, admin table delete/reorder teleports,
Contact/AppointmentBooking submit-state swap, AtsMatcher chip stagger —
see the original audit findings in conversation history for full detail.
